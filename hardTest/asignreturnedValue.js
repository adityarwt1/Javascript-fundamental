// Setup
let processed  = 2;

function processArg(num) {
  return (num + 3) / 5;
}

// Only change code below this line
processed = processArg(7);


/// mistake i made i learn in the when i learning this returning the value functionality to asing into the processed
/// i think i give any value in the num code line number 4, but we need to make equal value for asigning