const unflated = [1,2,[3,4,5],6,[7,8,9]]

const flattedarray= unflated.flat()

console.log(flattedarray)


/// when we are use fla method to make a clean arraay elements 
/// when we use flat() empty it removes emty slot from the array and 
/// when we use flat(1) making array flat level 1 only
/// when we use flat(infinity) make fully flat all time