const array = ["apple","papaya"]

const joinedarray = array.join("-")
console.log(joinedarray);

//the output when do that is apple-papaya 
//join mainly join the each element when the segment we are passing the method to joinning the array
