const createPerson = (name, age, gender) => {
    // Only change code below this line
    return {
      name, name,
      age, age,
      gender, gender
    };
    // Only change code above this line
  };

  /// we camn clearly seen we can easily access the name , name instead of using name: name